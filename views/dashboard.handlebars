<section class="user">
  {{!-- <img src="/images/{{painting.filename}}" alt="{{painting.description}}"> --}}
  <div class="user-details">
    <h2>ID: {{user.id}}</h2>
    <p>Username: {{user.username}}</p>
    <p>Email: {{user.email}}</p>
    <p>add all blogs or mabye comments </p>
  
  </div>
</section>